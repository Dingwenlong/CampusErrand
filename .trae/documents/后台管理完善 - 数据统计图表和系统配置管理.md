## 后台管理完善计划

### 当前状态分析
- ✅ 后台管理前端框架已搭建（Vue3 + Ant Design Vue + ECharts）
- ✅ Dashboard页面已有基础统计卡片
- ✅ 图表区域已预留，但使用静态数据
- ⚠️ 需要完善数据统计图表（对接真实数据）
- ⚠️ 需要实现系统配置管理功能

### 实现计划

#### 1. 数据统计图表完善

**后端开发**
- 创建 `AdminController` - 后台管理接口
- 创建 `AdminService` - 后台管理服务
- 添加统计接口：
  - `GET /admin/dashboard` - 获取仪表盘数据
  - `GET /admin/task-status-stats` - 任务状态分布
  - `GET /admin/amount-trend` - 交易趋势数据
  - `GET /admin/user-growth` - 用户增长趋势

**前端开发**
- 完善 `Dashboard/index.vue`
  - 对接真实数据
  - 动态更新图表
  - 添加更多统计维度

#### 2. 系统配置管理

**后端开发**
- 创建系统配置表 `tb_config`
- 创建 `Config` 实体类
- 创建 `ConfigMapper`
- 创建 `ConfigService` 和实现
- 创建 `ConfigController`
  - 获取配置列表
  - 更新配置
  - 获取单个配置

**前端开发**
- 完善 `Setting/index.vue`
  - 配置分类展示
  - 配置编辑表单
  - 配置保存功能

#### 3. 其他管理功能增强
- 用户管理：禁用/启用用户
- 任务管理：审核任务、查看详情
- 订单管理：查看所有订单
- 钱包管理：查看交易流水

#### 4. 数据表创建
```sql
-- 系统配置表
CREATE TABLE tb_config (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  config_key VARCHAR(100) NOT NULL COMMENT '配置键',
  config_value VARCHAR(500) COMMENT '配置值',
  description VARCHAR(200) COMMENT '配置描述',
  category VARCHAR(50) COMMENT '配置分类',
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_config_key (config_key)
);
```

请确认此计划后，我将开始实现具体代码。